这是一份经过深度讨论、多轮迭代并整合了所有核心技术细节（纯软件化、5G深度定制、VCC插件化、自动化安全分析、分布式运维）的最终版PRD文档。

---

# 《新型电力系统及虚拟电厂通信协议仿真模拟环境》产品需求说明书 (PRD)

| 项目名称 | 新型电力系统及虚拟电厂通信协议仿真模拟环境建设 | 版本 | V1.0 (最终定稿版) |
| :--- | :--- | :--- | :--- |
| **项目目标** | 构建全软件化、高仿真、具备安全攻防分析能力的虚拟电厂全业务链条模拟平台 | **状态** | 待评审/立项 |

---

## 1. 项目概览
### 1.1 项目背景
随着新型电力系统和虚拟电厂（VPP）的快速发展，电力工控协议（如IEC 61850, 104等）与5G、IoT技术的融合日益紧密。为了应对日益严峻的网络安全挑战，亟需一套不依赖物理硬件、支持大规模分布式部署、具备自动化漏洞捕获能力的实验仿真平台。

### 1.2 核心目标
*   **全软件定义：** 彻底去除物理硬件依赖，采用容器技术模拟PLC、RTU、逆变器等电力终端。
*   **5G安全链路：** 深度集成5G仿真，模拟信令加解密及业务切片对电力报文的影响。
*   **漏洞挖掘闭环：** 支持外部Fuzzing接入，实现崩溃现场（Core Dump）自动捕获及PoC脚本自动生成。
*   **全业务仿真：** 模拟“源-网-荷-储”业务流，支持复杂的点表映射与调度逻辑。

---

## 2. 系统总体架构
系统采用四层级联架构，支持跨物理机分布式部署：

1.  **VPP 主站层 (Master)：** 业务调度中心。技术栈：**Python (FastAPI)**。
2.  **控制协调中心层 (VCC)：** 逻辑聚合与协议转发中心。技术栈：**C++/Python 插件化框架**。
3.  **5G 网络仿真层：** 通信骨干。技术栈：**Open5GS + UERANSIM**。
4.  **终端仿真层 (Endpoints)：** 底层电力资产。技术栈：**C/C++ (基于底层开源协议栈库)**。

---

## 3. 功能模块详述

### 3.1 VPP 主站模拟器 (Python-based)
*   **核心功能：** 资源准入管理、调度指令下发（调峰/调频）、全局状态看板。
*   **安全中心：** 集成漏洞记录列表，支持一键调取崩溃现场的内存镜像与流量包。
*   **API 接口：** 提供标准RESTful API用于外部安全工具调用与环境管理。

### 3.2 插件化 VCC (控制协调中心)
*   **映射引擎：** 支持通过JSON/YAML点表配置，实现 IEC104 与南向 Modbus/MQTT/61850 等协议间的逻辑映射。
*   **逻辑聚合：** 接收主站总量指令，根据终端状态自动计算并分发具体执行指令。
*   **动态扩展：** 协议驱动以独立插件形式存在，支持在不重启主程序的情况下热加载新协议。

### 3.3 深度 5G 仿真集成
*   **安全策略定制：** 支持强制开启或关闭NEA/NIA加解密算法组合，用于安全穿透性对比分析。
*   **切片模拟：** 为不同电力业务（如GOOSE高优先级业务）配置独立的QoS保障与网络切片。
*   **时钟同步：** 全环境基于 **PTP (Linuxptp)** 授时，确保主站与成百上千个终端的时间误差控制在 **1ms** 以内。

### 3.4 自动化漏洞挖掘辅助系统
*   **Fuzzer 友好接口：** 支持 AFL++、Boofuzz 等工具接入仿真网络。
*   **Watchdog 监控：** 实时监控终端协议栈进程。发生 `Segfault` 时，5s 内自动捕获 **Core Dump** 并记录触发崩溃的 **PCAP 流量片断**。
*   **PoC 自动生成：** 系统可将成功的攻击流量自动封装为标准的 Python PoC 脚本，实现“一键复现”。

---

## 4. 电力业务仿真参数规格
各模块需按照下表模拟实时电力参数，支撑业务逻辑挖掘：

| 模块类型 | 关键模拟参数 (包括但不限于) |
| :--- | :--- |
| **电源侧 (Source)** | 有功/无功功率、三相电压/电流、逆变器效率、光照强度、风速、并网状态。 |
| **储能侧 (Storage)** | SOC (荷电状态)、SOH (健康度)、充放电功率给定、单体极值电压、BMS告警位。 |
| **需求侧 (Demand)** | 负荷功率、优先级标签、EV充电桩连接状态、室内外设定温度。 |
| **VCC 聚合层** | 区域总可调容量、在线率、5G链路平均延迟与抖动。 |

---

## 5. 技术约束与运维要求

### 5.1 开发语言与性能指标
*   **语言选型：** 主站(Python)、终端(C/C++ - 确保内存漏洞真实性)、VCC(混合架构)。
*   **模拟规模：** 在标准 32C/64G 服务器集群下，支持 $\ge 200$ 个具备独立5G身份（UE）的仿真终端并发运行。
*   **响应时延：** 主站下发指令到终端响应的端到端逻辑延迟 $\le 200ms$。

### 3.2 部署与监控
*   **双模部署：** 支持 `Docker-compose` (单机开发版) 与 `Kubernetes + Helm` (集群科研版)。
*   **分布式存储：** 采用分布式存储（如 Minio）存放全网汇聚的漏洞复现数据（Core/PCAP）。
*   **全栈观测性：** 集成 **Prometheus + Grafana** (电力指标监控) 与 **EFK** (日志审计与崩溃关联分析)。

---

## 6. 安全测试与验证流程
1.  **攻击准备：** 研究员接入现有工具或编写 Scapy 脚本，通过 5G 仿真链路向终端发送畸形报文。
2.  **崩溃探测：** Watchdog 监测到终端进程异常结束。
3.  **快照存证：** 系统自动保存崩溃瞬时的内存 Core 文件、上下文日志及 5G 隧道加密流量包。
4.  **复现闭环：** 导出 Python PoC 脚本，在洁净环境重新运行，验证漏洞是否可稳定重现。

---

## 7. 验收标准与交付物

### 7.1 验收 KPI
*   **全链路通过：** 演示“主站指令 -> 5G隧道 -> 终端执行 -> 状态回传”业务流闭环。
*   **能力闭环：** 提供 1 份针对 IEC 104 或 61850 协议的漏洞深度挖掘全流程记录（含触发与复现）。
*   **仿真精度：** 证明 5G 加解密策略对报文形态的真实改变，证明时钟同步误差符合标准。

### 7.2 交付清单
1.  **系统安装包：** 全套 Docker 镜像、K8s 部署脚本、5G 核心网配置文件。
2.  **源代码：** VPP 主站源码、VCC 插件框架源码、终端协议桩源码。
3.  **技术文档：** 《系统集成与运维手册》、《协议点表映射配置指南》、《漏洞挖掘复现技术报告》。
4.  **PoC 脚本库：** 包含不少于 3 套针对不同协议（104/Modbus/MQTT）的自动化验证脚本。

---

**（文档完结）**
